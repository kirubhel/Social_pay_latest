// Middleware file kept for reference but internationalization removed
export default function middleware() {
  // No internationalization middleware
}

export const config = {
  matcher: [] // No paths to match
}; 